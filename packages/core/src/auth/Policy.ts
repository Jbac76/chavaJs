/**
 * Base policy class — Laravel's `Illuminate\Auth\Access\Policy`.
 * Policies are plain classes with ability methods:
 *
 *   export class PostPolicy {
 *     public update(user: User, post: Post): boolean {
 *       return user.getKey() === post.getAttribute('user_id');
 *     }
 *   }
 */
export class Policy {}
